fix(pet): 思考表情改按"推理段"计时 —— 恍然大悟在长思考段结束后触发,一次任务可多次(0039)

- 主进程 connection.ts 新增推理段检测:assistant/chunk 的 reasoning 块进入 = 段开始,
  非 reasoning 块(text/tool-call)开始、step/end、turn/end = 段结束;发射
  dsh:thinking-start / dsh:thinking-end(一次 turn 可含多段,各段独立触发)
- animator:删除 turn 级计时,新增 onThinkingSegmentStart/End —— 段时长 >=A 段末
  播放恍然大悟一次;>B 段内循环困惑;段末先停困惑再恍然大悟;恍然大悟自然播完、
  turn 仍在 thinking 时恢复思考姿态(onEnd);离开 thinking 时恍然大悟在播则让它播完
- 主进程 turn/end 先收尾未关闭的推理段再发 turn-end(顺序保证 renderer 先判段后切状态)
- 设置文案/注释同步为"推理段"语义;默认 A=5s、B=15s 不变
