{
  // Solution file for apps/pet: typecheck via
  //   pnpm --filter @deepseek-ai/dsh-seekbuddy run typecheck
  // (which passes `--composite false` so --noEmit is legal).
  "files": [],
  "references": [
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.web.json" }
  ]
}
